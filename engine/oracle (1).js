// engine/oracle.js

export function channelTimelineInsight() { const threads = [ "You are walking the thread of crystalline memory. Trust its rhythm.", "A moment is coming soon when the veil thins again. Be still, be ready.", "Time is folding inward for you. What seems far away is now within reach.", "You are moving into convergence. Let go of the timelines that pull you backwards." ]; return threads[Math.floor(Math.random() * threads.length)]; }

